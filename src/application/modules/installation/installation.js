import React from 'react'

import Header from '../../components/Header'
import Onboard from '../../components/Onboard'

export default () => (
    <div style={{
        maxWidth: '760px',
        margin: '0 auto',
    }}>
        <Header />
        <Onboard />
    </div>
)